export function send(t,r,e){t.statusCode=r,t.setHeader("Content-Type","application/json; charset=utf-8"),t.setHeader("Cache-Control","no-store"),t.end(JSON.stringify(e))}export async function readBody(t){if(t.body&&"object"==typeof t.body&&!Buffer.isBuffer(t.body))return t.body;if("string"==typeof t.body)try{return JSON.parse(t.body)}catch{return{}}const r=[];for await(const e of t)r.push(e);if(!r.length)return{};try{return JSON.parse(Buffer.concat(r).toString("utf8"))}catch{return{}}}export function queryOf(t){if(t.query&&"object"==typeof t.query)return t.query;try{const r=new URL(t.url,"http://localhost");return Object.fromEntries(r.searchParams.entries())}catch{return{}}}
